1. Основные функции

Загрузка одежды

Пользователь может загрузить фото одежды:

📸 собственные вещи (сфотографированные телефоном),

🛍 вещи из маркетплейсов (скриншоты/фото из интернет-магазинов).

Нейросеть автоматически обрабатывает изображения:

вырезает фон,

нормализует размеры,

помещает вещи в виртуальный гардероб.

Создание виртуального гардероба

Вещи сохраняются в профиле пользователя, с возможностью:

добавлять теги, категории, названия,

сортировать и фильтровать.

Подбор образов (Look Builder)

Интерфейс в виде карусели:

подбор элементов сверху вниз (головные уборы → верх → низ → обувь → аксессуары).


Генерация готового образа c всеми предметами размещёнными на одном изображении.

AI-генерация образов

Пользователь выбирает:

собранный образ,

окружение (город, помещение, ночь, дождь, туман и т.п.).

Заготовленного манекена(персонажа)

Нейросеть генерирует финальное фото с полной стилизацией.

Управление образами

Образы сохраняются в профиле, где есть:

коллекции/папки (пользователь сам задаёт категории),

функции: редактировать, переименовать, удалить, поделиться.


2. Архитектура приложения

Frontend (клиентская часть):

Мобильное приложение (iOS/Android, возможно на React Native).

Интерфейс: загрузка фото, карусель выбора одежды, просмотр и редактирование образов.

Backend (серверная часть):

Авторизация и хранение профиля.

Управление гардеробом и образами.

API для работы с нейросетями.

AI-модули:

Обработка изображений одежды: вырезка, нормализация.

Виртуальная примерка: наложение на аватар с контекстных изображений.

Генерация финальных фото (Diffusion models, GAN и т.п.).

База данных:

PostgreSQL/MySQL для структурированных данных (пользователи, вещи, образы). 
/ Облачные сервисы BaaS

Object storage (S3/Cloudinary) для хранения фото. / Облачные сервисы BaaS

3. Пример пользовательского сценария

Пользователь фотографирует свою футболку → она сохраняется в гардеробе.

В «Look Builder» он выбирает футболку + джинсы + кроссовки.

Сохраняет образ в папку «Повседневка».

Хочет посмотреть, как будет выглядеть в Париже ночью → запускает AI-генерацию.

Получает финальное изображение, где он в собранном луке стоит у Эйфелевой башни.